@model IEnumerable<DWL_CRM.Models.Geschaeftsfuehrer>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Person)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Person.PersonId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.GeschaeftsfuehrerId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.GeschaeftsfuehrerId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.GeschaeftsfuehrerId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
